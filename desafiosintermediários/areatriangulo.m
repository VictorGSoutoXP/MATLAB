// Criar uma função que calcula a área de um triângulo, dada a base e a altura.

function area = calcular_area(base, altura)
    area = (base * altura) / 2;
end

// Para usar a função:

area = calcular_area(10, 5); % chama a função com base = 10 e altura = 5
